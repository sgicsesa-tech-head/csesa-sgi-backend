SECRET_KEY=supersecret
# For quick local start, SQLite is used by default. To use MySQL/Postgres, set DATABASE_URL accordingly.
# DATABASE_URL=mysql+pymysql://user:password@localhost/csesa

ADMIN_EMAIL=admin@csesa.in
# Prefer hashed password (pbkdf2:sha256). Generate with Python:
# from werkzeug.security import generate_password_hash; print(generate_password_hash('yourpass'))
ADMIN_PASSWORD_HASH=
# Fallback plaintext for dev only (leave empty if using hash)
ADMIN_PASSWORD=

MAIL_EMAIL=csesa@gmail.com
MAIL_PASSWORD=change_me

# Razorpay (test keys)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
